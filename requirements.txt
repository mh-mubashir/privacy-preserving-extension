# Core dependencies for privacy-preserving adversarial training
# Python 3.9+ recommended

torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0

# Optional: VAE variants and training utilities (DisentangledBetaVAE, BetaTCVAE, etc.)
# Used by pythae-based training scripts.
pythae>=0.1.0

# Optional: for Weights & Biases logging (--use_wandb)
wandb>=0.15.0

# Optional: for CelebA automatic download (--download)
# gdown>=4.7.0

# Optional: for CelebA automatic download (--download)
# gdown>=4.6.0

# Optional: for MACs/FLOPs profiling (models/unet.py __main__)
# torchprofile>=0.1.0
