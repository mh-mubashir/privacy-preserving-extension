# Privacy-Preserving Edge Vision -- Python dependencies
# Python 3.9+ recommended. Install with: pip install -r requirements.txt
#
# GPU note: install PyTorch with CUDA wheels separately from pytorch.org
# e.g. CUDA 12.8: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128

# --- Core ---
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0

# --- Evaluation (evaluate.py) ---
scikit-learn>=1.0.0       # roc_auc_score, f1_score, confusion_matrix

# --- CelebA via HuggingFace (--data_source huggingface) ---
datasets>=2.0.0            # flwrlabs/celeba dataset
Pillow>=9.0.0              # image decoding for HF CelebA rows

# --- Compute profiling (compute_flops.py, export_onnx.py, benchmark_latency.py) ---
thop>=0.1.0                # MACs / parameter count
onnx>=1.14.0               # ONNX export (export_onnx.py)
onnxruntime>=1.16.0        # CPU latency benchmarking (benchmark_latency.py)

# --- Pythae prototyping (pythae_training.py) ---
pythae>=0.1.0              # DisentangledBetaVAE, BetaTCVAE, FactorVAE variants

# --- Optional ---
wandb>=0.15.0              # Weights & Biases logging (--use_wandb)
# gdown>=4.7.0             # CelebA download via torchvision (--download); may hit quota
